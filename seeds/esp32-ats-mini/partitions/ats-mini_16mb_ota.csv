# 16 MB flash, dual-slot OTA. Deliberately no `factory` partition: PlatformIO
# always flashes the app to 0x10000 and boot_app0.bin (ota_seq = 1 -> slot 0) to
# 0xE000, so the app at 0x10000 must BE ota_0, otherwise the bootloader selects
# an empty or stale slot. Mirrors the arduino-esp32 default_16MB.csv convention.
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xE000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x400000,
app1,       app,  ota_1,    0x410000, 0x400000,
spiffs,     data, spiffs,   0x810000, 0x7E0000,
coredump,   data, coredump, 0xFF0000, 0x10000,
