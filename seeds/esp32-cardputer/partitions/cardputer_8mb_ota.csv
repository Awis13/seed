# 8 MB flash, dual-slot OTA. Deliberately no `factory` partition: PlatformIO
# always flashes the app to 0x10000 and boot_app0.bin (ota_seq = 1 -> slot 0) to
# 0xE000, so the app at 0x10000 must BE ota_0, otherwise the bootloader selects
# an empty or stale slot. Mirrors the arduino-esp32 default_8MB.csv convention.
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xE000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x330000,
app1,       app,  ota_1,    0x340000, 0x330000,
spiffs,     data, spiffs,   0x670000, 0x180000,
coredump,   data, coredump, 0x7F0000, 0x10000,
