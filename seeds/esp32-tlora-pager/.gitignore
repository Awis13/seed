# MeshCore identity secrets (never commit)
data/mesh_identity.id
data/mesh_pair.json
data/mesh_probe_s.txt
# WireGuard + WiFi secrets (never commit)
data/wg.json
data/wifi.json
data/auth_token.txt
# Gateway capability token + agent bridge URL (never commit)
data/gw_token.txt
data/agent_bridge.txt
